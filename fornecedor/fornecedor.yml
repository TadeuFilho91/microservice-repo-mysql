spring:
  datasource:
    password: Evaristo
    username: root
    url: jdbc:mysql://localhost/fornecedor?createDatabaseIfNotExist=true&serverTimezone=UTC
  jpa:
    hibernate:
      ddl-auto: update
